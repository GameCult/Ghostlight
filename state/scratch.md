# Scratch Working Memory

## Current Subgoal

Claude connector (operator order, 2026-09-06), design not decided. Both
opening assumptions are void (no API key, nothing into CodexConnector; the
Modeling map `modeling-claude-connector.md` is void). Premise: Claude
subscription through the Agent SDK or Claude Code CLI as a stopgap, a separate
small organ. Gate: the read-only research pass `research-agent-sdk.md` (can the
SDK return tool calls unexecuted) and the operator saying go. Nothing edited in
either repo at `06236a4`.

## Working Notes

Use this file for one bounded slice. Delete or reset aggressively when the
slice is done.
