{
  "name": "ghostlight-claude-sdk-sidecar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ghostlight's Claude Agent SDK sidecar. Reads length-prefixed MessagePack frames on stdin, runs one SDK query, and answers every tool call by asking Rust. It computes nothing and holds no credential.",
  "engines": { "node": ">=24 <25" },
  "scripts": {
    "build": "tsc -p .",
    "test": "node --experimental-strip-types --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.261",
    "@msgpack/msgpack": "3.1.3",
    "zod": "4.5.4"
  },
  "devDependencies": { "@types/node": "24.13.3", "typescript": "5.9.3" }
}
